---
  
apt_packages:
  - awscli
  - bash
  - build-essential
  - ca-certificates
  - curl
  - default-jre
  - git
  - openssl
  - python3
  - ssh
  - ssh-client
  - vim
  - wget
  - zsh

docker_packages:
  - apt-transport-https
  - aptitude
  - software-properties-common
  - python3-pip
  - virtualenv
  - python3-setuptools

postgres_packages:
  - acl
  - libpq-dev
  - libssl-dev
  - libssl-doc
  - postgresql
  - postgresql-contrib
  - python3-psycopg2

user: ubuntu

db_name: ubuntu
db_password: ubuntu

git_username: git-user
git_email: git-user@email.com
